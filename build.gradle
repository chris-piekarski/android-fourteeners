// Modernized build - plugin versions managed in settings.gradle
// Intentionally minimal: no buildscript or repositories needed here.

tasks.register("clean", Delete) {
    delete(rootProject.buildDir)
}
